# Frontend Development Server
# These can be overridden by environment variables

# Vite dev server port (default: uses SERVER_PORT from root .env or 3000)
# VITE_PORT=3000

# Vite dev server host
# VITE_HOST=0.0.0.0

# Backend server for proxy (default: uses SERVER_PORT/SERVER_HOST from root .env)
# VITE_BACKEND_HOST=localhost
# VITE_BACKEND_PORT=8080
