# Environment
ENV=local_dev

# Debug mode
DEBUG=1

# Backend Server configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8085

# Data paths (defaults are relative to working directory)
# For systemd install, these will be under /var/lib/scanimage-server/
DATA_PATH=/var/lib/scanimage-server/data
SCANNED_PATH=/var/lib/scanimage-server/files/scanned
ARCHIVES_PATH=/var/lib/scanimage-server/files/archives
PDF_PATH=/var/lib/scanimage-server/files/pdf
CACHE_PATH=/var/lib/scanimage-server/cache

# Frontend dev server (optional, for development)
# If not set, frontend will use SERVER_PORT for backend proxy target
# VITE_PORT=3000
# VITE_BACKEND_PORT=8085
